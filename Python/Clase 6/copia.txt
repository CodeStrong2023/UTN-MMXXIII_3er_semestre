Programamos con diferentes tipos de archivos, ahora en txt.
Los acentos son importantes para las palabras
como por ejemplo: acción, ejecución y producción
las letras son: r (read), a (append), w (write), x (crea un archivo)
t (para texto o text), b (archivos binarios), w+ (lee y escribe), r+(escribe y lee)
Saludos al profesor
Con esto terminamos.